name: System Check

on: [push]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run git-summary
        run: |
          chmod +x git-summary.sh
          ./git-summary.sh
